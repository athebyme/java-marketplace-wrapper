package org.example.domain.model;

import org.example.domain.model.abstractions.Marketplace;

public class Wildberries extends Marketplace {
    public Wildberries(int id, String name) {
        super(id, name);
    }
}
